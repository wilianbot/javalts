package Aula8;

public class character1 {
    // verificar se é digito ou letra
    char c = '5';
    if (Character.isDigit(c))
        System.out.println(c + "É digito: ");
    if (Character.isLetter(c))
        System.out.println(c + "É letra: ");
}
